export type FilterValues = {
    type?: string
    apartment_type?: string
    bhk?: string[]
    price?: string
    area?: string                // ✅ fixed typo
    preferredTenants?: string    // ✅ fixed typo
}
